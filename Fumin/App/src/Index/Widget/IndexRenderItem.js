import React, {Component} from 'react';

import {View, StyleSheet, Text} from 'react-native';
const IndexRenderItem = ({
  item,
  onPress
}) => {

  return (
    <View
  >
     
    </View>
  );
};
export default IndexRenderItem;

const styles = StyleSheet.create({

});
